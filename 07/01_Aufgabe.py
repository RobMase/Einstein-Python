

class Wuerfel:
    def wuerfeln(self):
        # die funtkion wuerfeln soll eine zahl zwischen 1 und 6 ausgeben

class Spiel:
    def __init__(self, anzahl_wuerfel):
        # Man soll ein neues Spiel-Objekt mit z.B. mit "Spiel(3)" oder "Spiel(4)"" erstellen koennen.
        # Wenn ein neues Spiel-Objekt erstellt wird, sollen so viele Wuerfel wie angegeben in eine Liste von wuerfeln erstellt und gespeichert werden

    def wuerfelAlle(self):
        # Wird die Funktion wuerfelAlle() aufgerufen, sollen alle Wuerfel einmal gewuerfelt werden.
